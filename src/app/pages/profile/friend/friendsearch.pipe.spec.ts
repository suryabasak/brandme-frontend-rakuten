import { FriendsearchPipe } from './friendsearch.pipe';

describe('FriendsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
